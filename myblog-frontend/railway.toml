[build]
builder = "DOCKERFILE"
dockerfilePath = "myblog-frontend/Railway.Dockerfile"
watchPatterns = ["myblog-frontend/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/"
healthcheckTimeout = 120
